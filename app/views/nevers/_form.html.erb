<%= form_with(model: never) do |form| %>
  <% if never.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(never.errors.count, "error") %> prohibited this never from being saved:</h2>

      <ul>
        <% never.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :body, style: "display: block" %>
    <%= form.text_field :body %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
